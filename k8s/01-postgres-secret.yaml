apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: sample-crud
type: Opaque

# ==========================================================
# LEGACY RAW POSTGRES CREDENTIALS
#
# Used only by the old StatefulSet PostgreSQL path:
# - 02-postgres-configmap.yaml
# - 03-postgres-statefulset.yaml
# - 04-postgres-service.yaml
#
# The current backend configuration in 05-backend-deployment.yaml
# points at CNPG and uses 20-cnpg-postgres-secret.yaml instead.
#
# Keep this file only when testing the non-CNPG PostgreSQL setup.
# ==========================================================
stringData:
  POSTGRES_USER: sampleuser
  POSTGRES_PASSWORD: samplepass

# Demo-only credentials. Replace with externally managed secrets
# before using this legacy path in a real environment.
